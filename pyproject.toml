[project]
name = "cleanstream"
version = "0.1.0"
description = "CleanStream Ad-Free Video Player"
requires-python = ">=3.9"
dependencies = [
    "flask",
    "flask-cors"
]

[tool.vercel]
entrypoint = "api.index:app"
